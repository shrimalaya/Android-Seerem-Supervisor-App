<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.supervisor_seerem">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.AdditionalInfoActivity"
            android:label="Additional Information"></activity>
        <activity android:name=".UI.UIPreferencesActivity"
            android:label="UI Preferences"/>
        <activity android:name=".UI.SensorsUsageActivity"
            android:label="Device Sensor Usage"/>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/maps_api_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".UI.WorkerInfoActivity"
            android:label="Worker Information"/>
        <activity android:name=".UI.UserInfoActivity"
            android:label="User Information"/>
        <activity android:name=".UI.CommunicationActivity"
            android:label="Individual Communication"/>
        <activity
            android:name=".UI.SiteMapActivity"
            android:theme="@style/AppTheme_NoActionBar" />
        <activity android:name=".UI.SiteInfoActivity"
            android:label="Worksite Information"/>
        <activity android:name=".UI.LoginInfoActivity"
            android:theme="@style/AppTheme_NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>